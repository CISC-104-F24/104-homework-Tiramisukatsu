using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NewBehaviourScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Hello, world!");
        Debug.LogWarning("! ! ! ! ! ! ! ! ! ! ! ! ! ! !");
        Debug.LogError("You have done something terribly wrong");
    }

    // Update is called once per frame
    // Author: Nick-Name
    /*

    I have no questions....yet
    A game idea I have....is to create real, unpredicatable horror using an open-world map and beasts controlled by advanced AI, with a functional ecosystem like in Rain World
    A game development area I hope to learn more about is programming xd 

     */
    void Update()
    {
        
    }
}
